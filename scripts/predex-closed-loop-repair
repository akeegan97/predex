#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT_DIR/.venv/bin/python" \
  "$ROOT_DIR/python/eda/repair_closed_loop_actions.py" \
  --runs-root "$ROOT_DIR/runs" \
  "$@"
